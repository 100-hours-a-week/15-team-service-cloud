apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ai-secret
  namespace: production
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: ai-secret
    creationPolicy: Owner
  dataFrom:
  - find:
      path: /commitme/v3/prod/ai-server
      name:
        regexp: ".*"
    rewrite:
    - regexp:
        source: "/commitme/v3/prod/ai-server/(.*)"
        target: "$1"
