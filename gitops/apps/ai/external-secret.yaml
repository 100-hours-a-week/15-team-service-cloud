apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ai-secret
  namespace: production
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: ai-secret
    creationPolicy: Owner
  dataFrom:
  - find:
      path: /commitme/v2/prod/ai-server
      name:
        regexp: ".*"
    rewrite:
    - regexp:
        source: "_commitme_v2_prod_ai-server_(.*)"
        target: "$1"
