apiVersion: v1
kind: Service
metadata:
  name: team15-chat
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: team15-chat
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
